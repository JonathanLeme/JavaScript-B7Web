/* 
    "Math.around()" usado para arredondar um número;

    "Math.floor()" usado para força o arredondamento para baixo, isto é, mesmo que o número seja, por exemplo, 3.99;

    "Math.ceil()" usado para força o arredondamento para cima, isto é, mesmo que o número seja, por exemplo, 3.01;

    "Math.abs()" retorna o número absoluto;

    "Math.min()" retorna o menor número da lista;

    "Math.max()" retorna o maior número da lista;

    "Math.random()" retorna um número aleatório entre 0 e 1;

    "Math.floor(Math.random() * 100)" exemplo de como usar 'random' em conjunto com 'floor'.
*/

let novoValor = Math.floor(Math.random() * 100);

console.log(novoValor);



