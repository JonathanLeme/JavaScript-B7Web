let n = 10;

let resultado = n.toFixed(2);

console.log(resultado);

/* 
    "?.toString()" transforma um 'number' em 'string';

    "parseInt(?)" transforma uma 'string' em 'number'. Só trabalha com números inteiros;

    "parseFloat(?)" preserva os números depois da vírgula;

    "?.toFixed(x)" usado para definir a quantidade de número que gostaria de aparecer depois da vírgula;    
*/