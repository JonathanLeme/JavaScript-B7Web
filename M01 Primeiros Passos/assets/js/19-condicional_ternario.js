let isMember = false;

// Exemplo de "Condicional Ternário" entre paranteses.

let shipping = (isMember ? 2 : 10);

// Outro exemplo de "Condicional Ternário" entre paranteses.

console.log(isMember ? 'Você é membro' : 'Você não é membro');
console.log("Frente: " + shipping);

let age = 60;

// Outro exemplo de "Condicional Ternário" entre paranteses.
// OBS: não é obrigado usar parenteses. É apenas uma forma de organização.

let isAdult = ((age >= 18 && age < 60) ? 'Você é um adulto' : 'Você não é um adulto');

console.log(isAdult);